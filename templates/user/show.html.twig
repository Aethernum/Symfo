{% extends 'base.html.twig' %}

{% block stylesheets %}
        <link href="{{ asset('css/compte_css.css') }}" type="text/css" rel="stylesheet" />
{% endblock %}

{% block title %}User{% endblock %}

{% block body %}
    <h2>Mes infos</h2>
    <div class="user-show">
        <div class="display-username">Nom d'utilisateur</div>
        <div class="user-username">{{ user.username }}</div>
    </div>
    <div class="user-mng">
        <a class="btn btn-primary" href="{{ path('user_edit', {'id': user.id}) }}">Modifier mes informations</a>
        <a class="btn btn-danger" href="{{ url("app_logout") }}">Deconnexion</a>
    </div>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
